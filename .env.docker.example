# Container Health Check Script
# This file contains a simple health check for the containerized application

# Environment Variables for Docker Container
# Copy this to .env if you need to set custom values

# Server Configuration
# PORT=5000

# Database Configuration (if using PostgreSQL for app data storage)
# DATABASE_URL=postgresql://user:password@host:5432/database
# PGHOST=localhost
# PGPORT=5432
# PGUSER=your_username
# PGPASSWORD=your_password  
# PGDATABASE=your_database

# Production Environment
NODE_ENV=production